# ── Secrets & runtime config ────────────────────────────────────────────────
# Your real config (tokens, passwords) — copy config.example.yaml → config.yaml
config.yaml
tokens/
cookies.txt
*.session
# Stray token/secret notes & exports — NEVER commit (this is taboo)
*token*.txt
*secret*.txt
*.arl
guest_links.json
guest_sessions.json
# Local portable bundles (shipped as release assets, not committed)
*-portable.zip
# OrpheusDL per-service secret blobs (Spotify/Tidal auth)
orpheus/
# Widevine L3 device — never publish (each user mints their own locally)
*.wvd
tools/widevine/
_widevine_setup/_keydive_out/
_widevine_setup/_sdk/
frida-server*
client_id.bin
private_key.pem
device_client_id_blob
device_private_key
# Windows / Go binaries (not part of the Python repo)
*.exe
backups/

# ── Downloaded media & generated output ─────────────────────────────────────
downloads/
mixed/
*.m4a
*.flac
*.mp3
*.opus
*.alac

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── Logs & caches ───────────────────────────────────────────────────────────
logs/
*.log
.cache/
releases_cache.json

# ── Desktop launcher / bundled-Python build artifacts ───────────────────────
RipsterLauncher.exe
RipsterSetup*.exe
build/
dist/
# Bundled Python embeddable (127 MB, rebuilt at package time — not committed)
python/
*.spec

# ── OS / editor noise ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
